server:
  http_port: 8080
  grpc_port: 9090

kafka:
  brokers:
    - localhost:9092
  topic: focus.events
  group_id: analytics-service

storage:
  backend: in-memory
  postgres:
    dsn: postgres://analytics:analytics@localhost:5432/analytics?sslmode=disable
